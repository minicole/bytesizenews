'use strict';

describe('myApp.article_page module', function() {

  beforeEach(module('myApp.article_page'));

  describe('article_page controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var article_pageCtrl = $controller('article_pageCtrl');
      expect(article_pageCtrl).toBeDefined();
    }));

  });
});