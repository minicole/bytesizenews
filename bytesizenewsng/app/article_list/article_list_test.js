'use strict';

describe('myApp.article_list module', function() {

  beforeEach(module('myApp.article_list'));

  describe('article_list controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var article_listCtrl = $controller('article_listCtrl');
      expect(article_listCtrl).toBeDefined();
    }));

  });
});